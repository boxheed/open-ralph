---
task_id: "0002-TEST"
validation_cmd: "npm run test"
affected_files: "package.json"
---

# Objective: Add a unit test coverage
When the unit tests are run the code coverage must be captured and reported.

## Success Criteria
1. When the npm run test command runs the the coverage should be reported on the console.
